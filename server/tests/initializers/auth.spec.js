describe('Auth Initializer', function(){
    var apiObj = {},
        should = require('should'),
        sinon = require('sinon'),
        initializer;

    beforeEach(function(){
        initializer = require('../../initializers/auth.js');
    });

    it('The initializer should do something', function(done){
        done();
    });

    it('The initializer should do something', function(done){
        done();
    });
});